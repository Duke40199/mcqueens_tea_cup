{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Mckween",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            // Point this to the folder containing your main.go
            "program": "./cmd/mckween/main.go", 
            // Important: Set CWD to root so it can find config.json and aliases.json
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": []
        }
    ]
}